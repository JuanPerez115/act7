/**
 * Color de fondo para el canvas.
 * @type {String}
 */
const COLOR_FONDO = "#e6f7ff";

/**
 * Color de los puntos que representan las ciudades en el canvas.
 * @type {String}
 */
const COLOR_PUNTOS = "#000000";

/**
 * Color de las lineas que representan una ruta en el canvas.
 * @type {String}
 */
const COLOR_LINEAS = "#8b8b8b";

/**
 * Imagen que representa el inicio de una ruta en el canvas.
 * @type {String}
 */
const PATH_IMG_START = "img/start2.png";

/**
 * Imagen que representa el final de una ruta en el canvas.
 * @type {String}
 */
const PATH_IMG_FINISH = "img/finish.png";